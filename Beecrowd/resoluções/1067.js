//const pegarValores = (line) => line.split(" ").map(a => Number(a));
let valor = Number(lines.shift())

for (let i = 0; i <= valor; i++) {
    if (i % 2 !== 0) {
        console.log(i);
    }
    
    
}